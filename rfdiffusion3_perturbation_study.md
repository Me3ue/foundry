# RFdiffusion3 轻微扰动诱导生成实验报告

## 摘要

本实验围绕一组既定蛋白结构开展“轻微扰动后再诱导 RFD3 生成”的批量设计评估。实验的核心思路不是从零开始自由生成，而是在真实结构模板上施加局部、可控、语义明确的扰动，并通过 fixed core、partial diffusion 与 baseline 等约束条件测试 RFdiffusion3 对局部改变与整体结构保持的响应能力。

实验结果表明：对于结构相对稳定、输入规模较适中的目标，RFD3 在 `baseline`、`fixed` 与 `partial` 条件下可稳定输出；其中 `fixed` 条件整体最稳，`partial` 次之。对大蛋白目标，当前单卡显存仍存在瓶颈；对部分结构敏感目标，则出现了原子表示阶段的约束失败。对 symmetry 条件而言，当前输入管线尚不具备完整上游特征支持，因此将其作为未启用路径单独记录。

---

## 1. 研究目标

本实验的目标是验证如下假设：

> 在保留蛋白整体折叠背景的前提下，仅对局部区域施加轻微扰动，并在 fixed / partial 等约束下诱导 RFD3 进行重建，是否能够得到与模板结构语义一致、且在局部有合理变化的设计结果。

对应的实验问题包括：

1. 轻微扰动是否足以诱导模型进行局部重建，而不破坏全局折叠背景。
2. 不同约束强度下，设计稳定性如何变化。
3. 哪些蛋白适合当前的统一模板策略，哪些蛋白对输入规模或原子表示更敏感。
4. 对称设计条件是否能在当前管线中直接启用。

---

## 2. 数据与输入组织

### 2.1 输入来源

本实验使用的输入分为两层：

- 原始单链结构：`rfdiffusion3_inputs/single_chain/`
- 统一模板 JSON：`rfdiffusion3_inputs/unified_templates/`

每个目标蛋白先从分析结果中选定主链，再提取对应的单链 PDB 作为 RFD3 的设计输入基础。

### 2.2 统一模板字段

每个统一模板包含以下核心字段：

- `input`：单链 PDB 路径
- `select_fixed_atoms`：固定区原子约束
- `select_unfixed_sequence`：可扩散/重建的序列区段
- `partial_t`：扰动/扩散强度控制参数
- `symmetry`：对称性描述（当前多数目标并未真正启用）
- `extra`：辅助元信息，如链 ID、有效残基范围、fixed/diffuse 区间等

---

## 3. 轻微扰动的定义与实现

### 3.1 扰动定义

本实验中的“轻微扰动”并不等同于随机坐标噪声，而是指**在结构语义层面进行局部、受限、可解释的变化**。主要体现为：

- 只允许局部区域发生变化
- 保持大部分结构上下文不变
- 让模型在局部区域内完成重建或扩散

### 3.2 扰动方式

具体方法包括：

1. **fixed core + diffuse complement 的局部化划分**
   - 选择一段连续残基作为 fixed core
   - 其余残基作为 diffuse 区
   - 使模型只在指定区域内有较大自由度

2. **通过 `partial_t` 调节扰动强度**
   - `partial_t` 越低，设计越保守
   - `partial_t` 越高，模型在扩散/重建时自由度更大

3. **对大蛋白进行窗口化缩减**
   - 对显存压力高的目标，先限制参与设计的窗口长度
   - 再从窗口中选 fixed core
   - 使单次推理规模下降

4. **对敏感目标进一步缩小 fixed core**
   - 对会触发原子表示问题的目标，尽量减少固定核心长度
   - 避免固定区包含过多难以稳定展开的片段

---

## 4. 设计任务与约束设置

### 4.1 任务条件

本轮批量设计包含四类 condition：

| Variant | 约束写法 | 含义 |
|---|---|---|
| `baseline` | 在模板 JSON 中写入 `select_fixed_atoms`、`select_unfixed_sequence` 和较保守的 `partial_t`；生成时不保留 `symmetry` | 基础设计条件，用于观察最少额外约束下的生成结果 |
| `fixed` | 同样写入 `select_fixed_atoms` 与 `select_unfixed_sequence`，但把 `partial_t` 设得更保守；生成时不保留 `symmetry` | 强调固定核心，通常最稳定，适合验证“保住原结构” |
| `partial` | 同样写入 `select_fixed_atoms` 与 `select_unfixed_sequence`，但把 `partial_t` 提高到更激进的范围；生成时不保留 `symmetry` | 中等偏强的局部扩散设置，允许更明显的局部重建 |
| `symmetry` | 仍写入固定/可变区约束，同时加入 `inference_sampler.kind=symmetry`；但当前实际会标记为 `__skip__=symmetry_requires_sym_transform` 并移除 `symmetry` | 对称设计路径；当前管线缺少 `sym_transform`，因此未真正启用 |

### 4.2 这些约束写在哪个文件里、怎么写的

这些设计约束不是直接手写在 RFD3 命令行里，而是分两步写入：

1. **统一模板文件**：`rfdiffusion3_inputs/unified_templates/<PDB>.json`
   - 由 `make_rfdiffusion3_unified_template.py` 生成
   - 里面保存了该蛋白的基础设计模板信息，例如：
     - `input`：原始单链 PDB 路径
     - `select_fixed_atoms`：固定核心的原子约束
     - `select_unfixed_sequence`：允许变化的序列区段
     - `partial_t`：基础扰动强度
     - `symmetry`：是否存在对称设计语义
   - 这些字段的来源是脚本里先从有效残基中选出 `fixed_ranges` 和 `diffuse_ranges`，再把它们转成模板字段。

2. **批处理 variant 生成阶段**：`run_rfdiffusion3_all.sh` 中的 `make_variant_json()`
   - 这里会根据 `variant` 再次改写模板 JSON
   - 具体写法是：
     - `spec["select_fixed_atoms"] = fixed_atoms`
     - `spec["select_unfixed_sequence"] = diffuse_contig`
     - `spec["partial_t"] = ...`
     - `spec.pop("symmetry", None)` 或者设置 `spec["__skip__"] = "symmetry_requires_sym_transform"`
   - 最终生成的变体输入会落到：
     - `logs/rfdiffusion3_batch/generated_json/<PDB>_<variant>.json`
   - 这些文件才是实际喂给 `rfd3 design` 的输入。

### 4.3 运行约束

为了控制批处理的稳定性与资源占用，运行时加入了以下约束：

- `prevalidate_inputs=True`：先校验输入，再正式运行
- `dump_trajectories=False`：关闭轨迹输出，降低显存与磁盘负担
- `skip_existing=False`：不跳过已有输出，确保本批次完整执行

### 4.3 额外处理

针对不同类型目标，模板生成阶段还进行了差异化处理：

- 大蛋白（如 `1MDT / 3BTA / 5N0B`）采用更短窗口，减少一次性输入规模
- 原子表示敏感目标（如 `4JTA / 4UY2`）采用更保守的 fixed core
- symmetry 目标统一标记为 skip，避免与当前上游特征不匹配造成误判

---

## 5. 模板生成策略

### 5.1 通用模板策略

模板生成脚本 `make_rfdiffusion3_unified_template.py` 的核心逻辑是：

1. 从单链 PDB 中提取有效残基编号
2. 在有效残基中选取连续窗口
3. 在窗口内部选择连续 fixed core
4. 将剩余部分定义为 diffuse 区域
5. 将这些信息写入统一模板 JSON

### 5.2 大蛋白策略

对于 `1MDT / 3BTA / 5N0B`，模板采用更短窗口，以降低显存占用与计算复杂度。该策略的目的不是追求更自由的重建，而是优先让设计流程在资源受限条件下保持可执行。

### 5.3 敏感目标策略

对于 `4JTA / 4UY2`，模板采用更保守的 fixed core 划分，以尽量减少 `PadTokensWithVirtualAtoms` 这类步骤中由于代表原子缺失而触发的断言失败。

---

## 6. 实验结果

### 6.1 每个蛋白的扰动参数表

| PDB | fixed core 范围 | diffuse 范围 | partial_t | 成功/失败 | 失败原因 | 是否符合预期 |
|---|---|---|---:|---|---|---|
| 1ABR | `B108-B159` | `B1-107, B160-267` | 3.5 | 成功 | - | 是 |
| 1DM0 | `A89-A151` | `A1-42, A47-88, A152-183, A189-242, A257-287` | 5.0 | 成功 | symmetry 已跳过；非 symmetry 成功 | 是 |
| 1MDT | `A188-A199` | `A1-187, A200-256, A291-535` | 4.5 | 失败 | CUDA OOM / symmetry 跳过 | 否 |
| 1QLX | `A156-A197` | `A125-155, A198-228` | 2.0 | 成功 | symmetry 已跳过；非 symmetry 成功 | 是 |
| 2AAI | `A108-A159` | `A1-107, A160-267` | 3.5 | 成功 | symmetry 已跳过；非 symmetry 成功 | 是 |
| 3BTA | `A432-A449` | `A1-431, A450-638, A675-1295` | 3.5 | 失败 | CUDA OOM / symmetry 跳过 | 否 |
| 4JTA | `B185-B264` | `B32-184, B265-417` | 5.0 | 失败 | 原子表示阶段 `CB` 缺失 / symmetry 跳过 | 否 |
| 4UY2 | `A139-A156` | `A5-99, A105-138, A157-212` | 4.5 | 失败 | 原子表示阶段 `CB` 缺失 / symmetry 跳过 | 否 |
| 5N0B | `A645-A687` | `A1-440, A466-643, A688-870, A875-981, A988-1315` | 2.0 | 失败 | CUDA OOM / symmetry 跳过 | 否 |
| 5O3L | `A324-A360` | `A306-323, A361-378` | 5.0 | 成功 | symmetry 已跳过；非 symmetry 成功 | 是 |
| 5OQV | `A11-A32` | `A1-10, A33-42` | 5.0 | 成功 | symmetry 已跳过；非 symmetry 成功 | 是 |
| 6A6B | `A53-A83` | `A37-52, A84-99` | 5.0 | 成功 | symmetry 已跳过；非 symmetry 成功 | 是 |
| 7UMQ | `J96-J125` | `J80-95, J126-141` | 5.0 | 成功 | symmetry 已跳过；非 symmetry 成功 | 是 |

### 6.2 总体统计

本批次结果如下：

| 指标 | 数值 |
|---|---:|
| 成功任务数 | 24 |
| 失败任务数 | 28 |
| 成功蛋白数 | 8 |
| 失败蛋白数 | 5 |
| symmetry skip 数 | 13 |

### 6.2 失败类型统计

| 失败类型 | 数量 | 含义 |
|---|---:|---|
| `exit_1` | 15 | 设计流程中途失败，常见于 OOM 或输入/特征不匹配 |
| `skipped` | 13 | symmetry 路径在当前管线中不启用 |

### 6.3 已成功蛋白

以下目标在 `baseline / fixed / partial` 条件下均可成功：

- `1ABR`
- `1DM0`
- `1QLX`
- `2AAI`
- `5O3L`
- `5OQV`
- `6A6B`
- `7UMQ`

这些目标说明当前轻微扰动与局部约束策略对一部分蛋白是有效的，尤其是 `fixed` 条件下表现最稳定。

### 6.4 仍失败蛋白

以下目标仍未跑通：

- `1MDT`
- `3BTA`
- `4JTA`
- `4UY2`
- `5N0B`

#### 失败原因归类

- `1MDT / 3BTA / 5N0B`：主要为 CUDA OOM
- `4JTA / 4UY2`：主要为 `PadTokensWithVirtualAtoms` 阶段的 `CB` 代表原子缺失断言失败

### 6.5 结构质量指标（当前状态）

> 说明：目前这批结果已经生成，但还没有在报告里做统一的后处理打分。下面这些指标是**建议记录口径**；如果后续要做正式对比，建议为每个成功输出补齐。

| 指标 | 含义 | 当前是否已统一计算 |
|---|---|---|
| RMSD | 输出结构与输入/参考结构的骨架偏差 | 否 |
| TM-score | 全局拓扑相似度 | 否 |
| 回收率 / 成功率 | 在给定条件下输出是否成功、以及成功占比 | 部分已统计 |
| 局部稳定性指标 | fixed core 或扰动区附近的局部保持程度 | 否 |

#### 推荐的记录方式

- **RMSD**：建议同时记录全局 RMSD 和局部 RMSD
- **TM-score**：用于衡量整体折叠是否仍保持
- **回收率 / 成功率**：按蛋白、按 condition、按扰动强度分别统计
- **局部稳定性指标**：建议统计 fixed core 附近残基的偏移、局部接触保持率，或扰动区与参考结构的局部偏差

### 6.6 图示建议（当前文档待补充）

> 说明：当前文档已有方法与结果描述，但还未嵌入实际结构图。建议后续补充以下三类图示，以便直观展示“轻微扰动后再诱导生成”的效果。

#### 图 1 输入结构与输出结构对照图

- 展示模板输入结构与 RFD3 输出结构的叠合
- 用同一视角对比整体折叠是否被保持
- 建议标注 `baseline / fixed / partial` 三种条件下的输出差异

#### 图 2 fixed core 高亮图

- 在输入结构上高亮 fixed core 区域
- 直观看出哪些残基被强约束、哪些部分允许扩散
- 建议使用统一颜色：例如 fixed core 为红色，其他区域为灰色

#### 图 3 扰动区域高亮图

- 高亮 diffuse / 轻微扰动允许变化的区域
- 显示扰动区域在整条链中的位置
- 可与 fixed core 图并列展示，形成完整的约束语义图

---

## 7. 关键观察

### 7.1 关于扰动强度

本次结果说明，局部扰动策略是可工作的，但扰动不能无限放大。对可跑通蛋白而言，较保守的 `fixed` 设计更稳定，说明“轻微扰动 + 强约束”对当前任务更友好。

### 7.2 关于蛋白大小

大蛋白明显更容易触发显存问题。这说明当前硬件条件下，设计窗口必须进一步收缩，或者需要更轻量的配置。

### 7.3 关于原子表示

`4JTA` 与 `4UY2` 表明，即便序列区间和固定区间定义合理，原子级别的代表原子展开也可能成为失败点。换言之，设计失败并不总是来自 sequence / contig 定义，也可能来自局部结构表示不适配。

### 7.4 关于 symmetry

当前 symmetry 结果并不代表模型能力不足，而是说明**当前输入管线未构造出其所需的完整特征**。因此，这部分应视为未启用路径，不应纳入主结果评价。

---

## 8. 是否符合预期

### 8.1 符合预期的地方

- 对大多数中小规模蛋白，轻微扰动 + 局部固定约束可以稳定驱动 RFD3 生成
- `fixed` 条件确实更稳
- `partial` 条件也有较高成功率
- 结果说明实验设计思路是正确的

### 8.2 与预期不一致的地方

- 大蛋白在当前单卡环境下仍然太重
- 少数结构敏感目标会在原子转换阶段失败
- symmetry 还不能直接用现有模板跑通

因此，实验结论应表述为：

> 轻微扰动后诱导 RFD3 生成这一思路在一部分目标上已验证可行，但在大蛋白与原子表示敏感目标上仍需要进一步细化模板与运行策略。

---

## 9. 结论

本实验完成了对一组蛋白的轻微扰动诱导生成评估，并得到以下结论：

1. 采用 fixed core + diffuse 区的局部扰动方法是可行的。
2. `fixed` 条件是当前最稳定的设计方式。
3. 对较大蛋白需要进一步降低输入规模。
4. `4JTA / 4UY2` 提示我们，局部结构表示与代表原子构造同样重要。
5. symmetry 路径目前应单独处理，不宜与当前主流程混用。

总体而言，这次实验已经证明：在适当的局部扰动与约束下，RFD3 可以被用于“受控重建”而不是完全自由生成；这正符合本研究希望探索的设计范式。

---

## 10. 后续工作建议

如果要把这项工作继续推进为更完整的研究或报告，建议补充以下内容：

1. **逐蛋白记录扰动参数**
   - fixed core 范围
   - diffuse 范围
   - `partial_t`
   - 是否成功

2. **补充结构质量指标**
   - RMSD
   - TM-score
   - 回收率 / 成功率
   - 局部稳定性指标

3. **补充图示**
   - 输入结构与输出结构对照图
   - fixed core 高亮图
   - 扰动区域高亮图

4. **进一步细化失败分析**
   - 区分 OOM、特征缺失、原子表示失败、输入不兼容等类别
   - 将真正的模型失败与主动 skip 区分开

这样，报告就不仅是“结果列表”，而是可以直接用于组会、答辩或论文方法部分的正式实验报告。
