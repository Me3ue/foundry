# @package _global_

defaults:
  - lora_finetune
  - _self_

name: lora-freeze-diffusion

ckpt_config:
  path: /backup01/zzj/protein/foundry/models/rfd3/rfd3_latest.ckpt
  reset_optimizer: true
  parameter_freezing_config:
    freeze_by_default: false
    param_policies:
      model.diffusion_module.*: true
