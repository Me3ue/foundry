# @package _global_
# Kept for compatibility, but this is no longer a multi-layer replacement.
# It factorizes a single linear. Use run_nmf_zkp_pdb_sweep.sh to iterate layers.

defaults:
  - nmf_zkp_single_layer_pdb
  - _self_

name: nmf-zkp-all-pdb
