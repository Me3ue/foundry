# @package _global_

defaults:
  - lora_finetune_pdb
  - _self_

name: lora-freeze-diffusion-pdb

ckpt_config:
  path: /home/zzj/protein/foundry/models/rfd3/rfd3_latest.ckpt
  reset_optimizer: true
  parameter_freezing_config:
    freeze_by_default: false
    param_policies:
      model.diffusion_module.*: true
